import io.cucumber.java.en.Given;
import org.example.Book;
import org.example.Library;

import java.util.ArrayList;
import java.util.List;

public class BookSteps {

    Library library = new Library();
    List<Book> result = new ArrayList<>();

    @Given(".+book with the title '(.+)', written by '(.+)'")
    public 

    /*
    public MyStepdefs() {
        And("^there is a book with the title \"([^\"]*)\" in the library$", (String arg0) -> {
        });
    }

    public MyStepdefs() {
        And("^the book \"([^\"]*)\" is already checked out to another member$", (String arg0) -> {
        });
    }

    public MyStepdefs() {
        And("^the book \"([^\"]*)\" should be available for borrowing$", (String arg0) -> {
        });
    }

     */


}
