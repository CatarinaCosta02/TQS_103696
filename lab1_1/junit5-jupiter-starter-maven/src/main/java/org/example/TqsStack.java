package org.example;

import java.util.Stack;

public class TqsStack {

    // create a Stack
    Stack<Integer> stack = new Stack<>();
    // do a push(x) operation
    stack.push(10);
    // do a pop() operation
    stack.pop();
    // do a peek() operation
    stack.peek();
    // size: returns the number os items in the stack
    stack.size();
    // isEmpty: returns true if the stack is empty
    stack.isEmpty();


}
